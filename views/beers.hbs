{{#each beersFromApi}}
    <div>
        <img src={{this.image_url}} alt={{this.image_url}}>
        <p>{{this.name}}</p>
        <p>{{this.description}}</p>
        <p>{{this.tagline}}</p>
    </div>
{{/each}}
